# Power Plan Backup Restore Notes

Phase 4 exports the active power plan and snapshots current CPU setting values. Future real active-plan writes must require this backup, verify current state, apply one selected setting, verify the result, and provide immediate restore.
