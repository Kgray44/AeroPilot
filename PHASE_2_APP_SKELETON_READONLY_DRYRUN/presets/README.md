# Phase 2 Presets

These files are preview-only examples for the future app. They are not applied by Phase 2.

Rules:

- CPU presets are JSON previews only.
- GPU profile mappings are unverified by default.
- Game automation rules keep `auto_apply` disabled.
- Any future apply phase must create backups and restore manifests first.
