# Restore Manifest Design

`restore/restore_manifest_latest.json` lists preview-only restore entries for power plan import, MSI file restore, and app JSON restore. Phase 4 does not execute these restore scripts except sandbox cleanup for the temporary clone.
