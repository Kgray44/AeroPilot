# Backup Manifest Design

`backups/backup_manifest_latest.json` records the active power plan export, current CPU values snapshot, MSI Afterburner file copies with hashes, app JSON backups, tool paths, NVIDIA telemetry snapshot, process snapshot, failures/skips, and Phase 5 readiness.
